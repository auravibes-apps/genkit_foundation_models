name: genkit_foundation_models_example
description: Example app for genkit_foundation_models.
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  genkit: ^0.13.2
  genkit_foundation_models:
    path: ..

dev_dependencies:
  dart_flutter_team_lints: ^3.0.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
